{
  "name": "omnipos",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/dashboard",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c cyan,magenta \"npm run dev -w apps/api\" \"npm run dev -w apps/dashboard\"",
    "build": "npm run build -w packages/types && npm run build -w apps/api && npm run build -w apps/dashboard",
    "test": "npm run test -w apps/api",
    "migrate": "npm run migrate -w apps/api",
    "lint": "npm run lint -w apps/api && npm run lint -w apps/dashboard",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.13.0",
    "@typescript-eslint/parser": "^7.13.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5"
  }
}
